# -*- Makefile -*-
#
# {project.authors}
# {project.affiliations}
# (c) {project.span} all rights reserved
#

# tell compilers to generate relocatable code
include shared/target.def

# the project name
PROJECT = {project.name}

# version info
PROJECT_MAJOR = 1
PROJECT_MINOR = 0

# project settings
PROJ_CXX_FLAGS = $(COMPILER_CXX_STD14)

# the repository
PROJ_REPO_ADMIN = {project.live.admin}
PROJ_REPO_HOST = {project.live.name}
PROJ_REPO_HOME = {project.live.home}
PROJ_REPO_REPOSITORY = $(PROJ_REPO_HOME)/repository

# the live host
PROJ_LIVE_HOST = {project.live.name}
PROJ_LIVE_ADMIN = {project.live.admin}
PROJ_LIVE_HOME = {project.live.home}
PROJ_LIVE_ROOT = {project.live.root}
PROJ_LIVE_WEB = {project.live.web}

# end of file
